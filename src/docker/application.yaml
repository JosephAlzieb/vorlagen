spring:
  flyway:
    schemas: database
    create-schemas: true
    url: jdbc:mariadb://localhost:3307/
    user: root
    password: iamgroot
  datasource:
    url: jdbc:mariadb://localhost:3307/database
    username: root
    password: iamgroot

